{
  "entities": {
    "User": {
      "title": "User",
      "description": "Stores user preferences and linked spreadsheet ID",
      "type": "object",
      "properties": {
        "spreadsheetId": {
          "type": "string",
          "description": "The Google Spreadsheet ID used for imports"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the spreadsheet was linked"
        }
      },
      "required": ["spreadsheetId", "updatedAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User specific settings and spreadsheet links"
    }
  }
}
